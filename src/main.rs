use cp_core::{lexical_analyzer::Cursor, *};
use cp_view::*;

fn main() {
    // let text = "()//dsds\n/*111*/int bcd 123 12e 12.2 12e-2 's' \"sdsd\t\" + - += |";
    // // let text = "_ab ds";
    // // lexical_analyzer::analyze(text);
    //
    // let mut c = Cursor::new(text);
    // while !c.is_eof() {
    //     match c.advance_token() {
    //         Ok(a) => println!("{}", a),
    //         Err(a) => println!("ERR: {}", a),
    //     }
    // }
    show();
}
